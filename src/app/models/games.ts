import { FakeBackend } from '../services/helper/fake-backend';

export class Game{
        image:string;
        namagame:string;
        deskription:string;
        participant:string;
        matchday:string;
        type:string;
        id:string;
        join:string;
}
